# audio/

The portfolio plays the MP3 in this directory through the native `<audio>`
element in `index.html`. Autoplay is attempted when the page loads; browsers
that block audible autoplay start it after the visitor's first interaction.
